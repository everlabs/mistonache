$("#modal-window").html("<%= escape_javascript(render 'shared/modal_window') %>");
$('.modal-title').text('<%= @person.name %>');
$('.modal-body .image').html('<%= image_tag @person.photo_url(:medium) %>');
$('.modal-body .description').text('<%= escape_javascript @person.description.html_safe %>');
