<div class="aside-section">
  <%= if @banners[:right].present?
        image_url = image_tag @banners[:right].image.url, class: 'banner-right', target: :_blank

        if not @banners[:right].url.empty?
          link_to image_url, @banners[:right].url, target: :_blank
        else
          image_url
        end
      end
  %>
</div>
