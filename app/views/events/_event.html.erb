<div class="col-lg-3 event">
  <%= image_tag event.image.url(:medium), class: 'event-poster' %>
  <h3 class="event-title"><%= event.title %></h3>
  <span class="event-inscription"><%= event.start_date %></span>
</div>